name: Verdicter policy check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  policy-check:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Run Verdicter scenarios
        uses: QuackaDuck/verdicter-action@v1
        with:
          api-key: ${{ secrets.VERDICTER_API_KEY }}
          api-url: https://www.verdicter.dev
